name: Dockhand - Infra Stack
on:
  push:
    branches: [ main ]
    paths:
      - 'infra-stack/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [ self-hosted, nixos ]
    steps:
      - name: Trigger Dockhand Webhook
        # Uses curl directly from the self-hosted runner to ping the internal IP
        run: |
          curl -sf "http://10.1.23.184:3000/api/git/stacks/1/webhook?secret=${{ secrets.INFRA_STACK_WEBHOOK }}" \
            -H "Content-Type: application/json"
